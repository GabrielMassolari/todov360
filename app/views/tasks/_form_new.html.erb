<%= simple_form_for task do |f| %>
  <%= f.input :name %>
  <%= f.input :priority, collection: [['Alta', 'alta'], ['Media', 'media'], ['Baixa', 'baixa']] %>
  <%= f.association :list, collection: current_user.lists.all%>
  <%= f.button :submit %>
<% end %>
